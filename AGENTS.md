# AGENTS.md

本文件是 `dr-card-ui` 仓库的全局智能体施工指南。适用于在本仓库内工作的各类 AI 编码/设计智能体。

## 1. 项目定位

- 本项目是「卡博士移动 APP 高保真 UI」项目，不是后台系统。
- 当前目标是快速、稳定地还原和沉淀移动端页面、交互状态与可复用组件。
- UI 项目优先保证视觉与交互正确，再考虑进一步工程化；不要为了抽象而抽象。
- 默认工作分支：`preview`。该分支接 Cloudflare Pages 自动预览部署，提交后可直接在移动端验收。除非任务明确指定其他分支，不要直接改 `main`。

## 2. 技术基线

当前工程基线：

- Vite
- React 18
- TypeScript
- React Router
- Tailwind CSS
- Lucide React

施工前先复用现有结构，尤其是：

- `src/components/ui/`
- `src/components/mobile/`
- `src/components/card/`
- `src/layouts/`
- `src/pages/`

不要在页面中重复造已有基础组件。

## 3. 设计系统

- 项目使用 Com Design 的金色 / 橙色体系 Token。
- Tailwind 已映射项目语义 Token；优先使用已有 Token 和语义类，不要随手硬编码一套新的颜色系统。
- 可以复用已经迁入本项目的 Com Design 组件和移动端公共组件。
- 若原型存在艺术化品牌素材、特殊会员卡、IP 插画等，应优先忠实还原原型视觉，不要为了组件统一把品牌特征抹平。

## 4. 视觉与需求事实源

### 4.1 摹客原型是主要视觉源

Mockplus（摹客）原型是本项目**大部分页面的主要视觉事实源**。

已经整理的原型语言文档位于：

- `docs/prototype/README.md`
- `docs/prototype/01-entry-and-home.md`
- `docs/prototype/02-membership-and-checkin.md`
- `docs/prototype/03-partner-and-invite.md`
- `docs/prototype/04-mall-card-order.md`
- `docs/prototype/05-account-message-service.md`
- `docs/prototype/06-deferred-and-legacy.md`
- `docs/prototype/07-page-index.md`

施工页面前，应先查对应的原型说明；需要核对具体布局、视觉、状态和交互时，以摹客原型本身为准。

注意：摹客页面树中的「首页」包含大量集中摆放的 artboard，它是原型总览工作台，不是一个需要直接实现成巨型 React 页面的真实业务页。

### 4.2 「不靠谱的设计历史」不是当前规范

仓库目录：

- `不靠谱的设计历史/`

这里保存的是历史设计资料。目录名就是警告：**这些文件不能被直接当成当前设计规范。**

其中大约有 10–20 个界面仍有参考价值，但需要重新讨论和确认。它们可能包含：

- 曾经正确、后来被改坏的版本；
- 与当前摹客原型不一致的视觉；
- 未最终确认的功能或布局；
- 可以继承的局部设计，但不能整页照搬的方案。

因此：

1. 只有历史设计有该页面，而当前原型没有足够依据时，**不要自行施工定稿**。
2. 当前原型与历史设计明显冲突时，**不要自行选择一个版本**。
3. 任务明确要求参考 `不靠谱的设计历史/` 中某个页面时，先区分“可借鉴部分”和“需要确认部分”。
4. 遇到上述情况，必须向用户说明具体页面、冲突点或缺失决策，并和用户讨论后再完成该页面。
5. 不要因为历史文件已经存在，就推断它已经获得产品/设计确认。

这条是项目级硬规则。

## 5. 需要讨论的页面怎么处理

遇到需要用户决策的页面时：

- 不要凭审美补齐关键布局、业务规则或主视觉方向。
- 明确指出当前能确认的部分、不能确认的部分，以及参考的是哪份历史设计。
- 优先提出少量、具体的设计决策问题，例如：页面主结构沿用哪版、保留哪个入口、某视觉模块是否继续使用。
- 只暂停存在决策阻塞的页面；若任务中还有不受影响的页面，可以继续完成其他部分。
- 用户确认后，以新的确认结论为最高优先级，并在必要时同步回设计文档或代码。

## 6. 原型差异与旧稿

原型中已经存在一些没有统一的表达，例如泡泡值/积分、不同兑换码长度、品牌名称差异，以及明确标记为“暂时不做 / 先不做”的页面。

规则：

- 不要在没有产品确认时擅自统一这些差异。
- `暂时不做 / 先不做` 的页面不能因为原型里存在就自动进入施工范围。
- `颜色测试`、实验页、旧稿等不属于正常业务页面，除非任务明确要求。

## 7. 页面施工原则

- 移动端优先，基准原型主要为 375 × 812。
- 首先保证信息层级、布局、间距、状态、主操作位置和品牌视觉接近原型。
- 页面应有合理的移动端响应能力，但不要为了桌面适配破坏手机端视觉。
- 图标优先使用 Lucide；若原型使用独有品牌/IP素材，则使用对应素材。
- 空态、加载态、成功态、禁用态等原型已经画出的状态应保留，不只实现一张静态首页。
- 不要随意改变原型文案来“优化产品”；发现文案矛盾时记录并提出，而不是静默修正。

## 8. 组件原则

- 先查现有组件，再新增。
- 可复用模式应沉淀到组件层；一次性的品牌艺术画面不必强行泛化。
- 公共组件保持 Token 化，避免页面私有颜色和重复 CSS。
- 复用组件时不能以“统一”为理由显著降低原型还原度。

## 9. 修改边界

- 只修改完成当前任务需要的文件。
- 不顺手重构无关代码。
- 不删除历史设计、原型资料或文档，除非任务明确要求。
- 不把 `不靠谱的设计历史/` 清理成“看起来更整洁”的目录；它本身是项目证据材料。

## 10. 提交前检查

至少完成与任务相匹配的检查：

```bash
npm run typecheck
npm run build
```

若只是纯文档修改，可不为了文档强行改业务代码，但仍需确认 Markdown 路径和引用没有明显错误。

提交信息应能直接说明本次施工内容，避免 `update`、`fix stuff` 一类无意义提交信息。

## 11. 决策优先级

发生冲突时，按以下顺序判断：

1. 用户在当前任务中的明确确认；
2. 已确认的当前摹客原型及其页面状态；
3. `docs/prototype/` 中基于摹客整理的原型语言；
4. 项目当前已经确认并实际使用的 Com Design Token / 公共组件约束；
5. 当前已经验收的页面实现；
6. `不靠谱的设计历史/` 中的历史参考；
7. 智能体自己的推断。

第 6、7 项不能越级替代前面的事实源。

---

一句话原则：**大部分页面跟着摹客把视觉做准；只有落在历史设计、原型缺口或冲突区的那些页面，不猜，拿出来和用户讨论。**
