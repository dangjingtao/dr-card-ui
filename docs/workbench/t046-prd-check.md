# T046｜PRD 检查报告

- 检查对象：`T046-dearseed-home-avatar-to-membership.md` 及其施工成果（工作区未提交改动）
- 检查日期：2026-09-11
- 检查方式：**独立复现**，不复用 `capture-t046.mjs` 的断言逻辑；脚本 `scripts/verify-t046-prd.mjs` 重新走真实登录动线取事实
- 环境：`vite preview` @ `http://127.0.0.1:5183`，系统 Chrome 通道，375×812

---

## 1. 结论摘要

**功能层面：施工成果成立，用户报告的问题已修复且可复现。**

**文档层面：PRD 有 2 条不能成立的条款 + 2 条描述失真，另有 1 项证据门槛未闭合。**

| 维度 | 结论 |
| --- | --- |
| 目标（3 条） | 2 条符合，1 条字面失真（描述性错误，非实现缺陷） |
| 实施要求（5 条） | 5 条符合（1 条 N/A） |
| 状态与交互矩阵（3 行） | 仅 1 行成立，2 行与实测不符 |
| 验收标准（5 条） | 5 条全部符合 |
| 必交证据（3 项） | 2 项符合，**提交号缺失 → 证据门槛未闭合** |
| 全局验收契约（6 条） | 3 条符合，3 条部分符合 |

**总体判定：功能通过，文档需修订后方可进入 `User Review`。**

> **2026-09-11 修订已执行**：卡内新增 `## 条款修订记录`，共修订 / 更正 7 处条款（R1–R7），详见 §5。剩余唯一阻塞项是「对应提交号」未回填。

---

## 2. 逐条核对

### 2.1 目标

| # | PRD 原文 | 实测 | 判定 |
| --- | --- | --- | --- |
| 1 | 头像 `onClick`/`to` 改为专栏「我的」路由 | `Home.tsx` 头像 → `/dearseed/membership` | 符合 |
| 2 | 跳转目标在 T051 改名落地后统一为 `/dearseed/membership` | 路由 `title`/`titleBarTitle` 均为「会员中心」 | 符合 |
| 3 | 不修改卡博士 APP 主壳头像行为（**仍指向 Profile 页**） | `/legacy-home` 头像 → `/membership`，本次未改动 | 行为符合，**字面失真** |

> 关于第 1 条：PRD 只写了「专栏首页」一个节点，实际涉及两个页面。`/`（`Home.tsx`）由本次修复；`/dearseed`（`DearseedColumn.tsx`）已由历史提交 `1e9fc2b`（2026-09-10，heyuxuan）完成。该提交的 message 明确记载了原目标是 `/profile` 而非 `/mall`。
>
> 关于第 3 条：PRD 写「仍指向 Profile 页」，但基线实测为 `/membership`。该描述在本次改动之前就已失真，非 T046 引入。

### 2.2 实施要求

| PRD 要求 | 实测 | 判定 |
| --- | --- | --- |
| 定位顶部头像按钮的跳转 handler | `src/pages/Home.tsx:93-101` | 符合 |
| 替换跳转目标为 `/dearseed/membership` | 已替换 | 符合 |
| 跳转前后视觉无变化，标题栏保持原样 | 仅改 `aria-label` / `data-*` / `onClick`，`className` 未动；标题栏仍「诗得丽品牌专栏」 | 符合 |
| 若存在「已登录/未登录」分支则保持不变 | `Home.tsx` 无此分支；守卫在 `MobileLayout`（T037） | N/A |
| 增加可见测试锚点 | `data-dearseed-avatar`，运行时 `count=1`、`visible=true` | 符合 |

### 2.3 状态与交互矩阵 —— 本节问题最集中

| 行 | PRD 声明 | 实测 | 判定 |
| --- | --- | --- | --- |
| 已登录 | 头像 → `/dearseed/membership` → 会员中心 | `/` → `/dearseed/membership`，标题栏「会员中心」 | **符合** |
| 未登录 | 头像 → `/login?from=/dearseed/membership` → 登录页 | `/` 被 T037 守卫 `navigate('/legacy-profile/login', { replace: true })` 整体重定向，**无 `from` 参数，且头像在未登录态不可达** | **不符合（不可实现）** |
| 会员中心内 | 任何返回按钮 → 回 `/dearseed` | `navigate(-1)` 浏览器历史回退 → 回到 `/` | **与字面不符** |

第 3 行的实测行为在 UX 上更正确（返回到来处），PRD 写 `/dearseed` 是建立在「专栏首页 = `/dearseed`」这一错误前提上。

### 2.4 验收标准

| 标准 | 实测 | 判定 |
| --- | --- | --- |
| 点击头像可进入会员中心 | 已复现 | 符合 |
| 不再跳转商城 | 全库 `navigate('/mall')` / `to='/mall'` **0 处** | 符合 |
| 主壳头像行为不受影响 | `/legacy-home` 头像仍 → `/membership` | 符合 |
| `typecheck` / `build` 通过 | `tsc --noEmit` 退出码 0；`npm run build` 通过 | 符合 |
| 375×812 截图对照 | 3 张 PNG，逐张核验尺寸均为 375×812 | 符合 |

### 2.5 必交证据

| 证据 | 状态 |
| --- | --- |
| 头像区域 375×812 截图 | ✅ `t046-01-home-avatar-before.png` |
| 跳转前后两屏对照 | ✅ `t046-01` → `t046-02`（另附 `t046-03` 返回） |
| **对应提交号** | ❌ 卡内写「待用户确认后提交回填」——**未闭合** |

`task-ledger.md` 第 4.5 节要求「每张页面卡至少提交……对应提交号」。该项缺失，因此卡片停在 `Agent Review` 是正确的，**不具备进入 `User Review` 的条件**。

### 2.6 全局验收契约（`tasks/README.md`）

| # | 契约 | 判定 | 说明 |
| --- | --- | --- | --- |
| 1 | 原型覆盖 | ⚠️ 部分 | 单节点已映射；但全卡**未引用任何 Mockplus 节点号**，不满足 `task-ledger` 4.1「明确引用对应 Mockplus 节点」 |
| 2 | 视觉证据 | ⚠️ 部分 | 375×812 齐备；缺「原型定位信息」 |
| 3 | 状态证据 | ⚠️ 部分 | 矩阵第 2 行不可实现、第 3 行不符 |
| 4 | 交互证据 | ✅ | 触发器 / 动作 / 目标 / 返回路径 / 实际结果均有记录 |
| 5 | 工程证据 | ✅ | typecheck、build、控制台无阻塞错误。⚠️ 「直达刷新」一项：`/` 整页刷新即因内存态登录丢失被守卫拦截——T037 既有行为，非本卡引入 |
| 6 | 用户验收 | ✅ | 卡片止于 `Agent Review`，未自标 `Accepted` |

---

## 3. 偏差清单

### P1（进入用户验收前应处理）

| 编号 | 偏差 | 影响 |
| --- | --- | --- |
| D1 | 矩阵第 2 行「未登录 → `/login?from=…`」不可实现 | 验收依据与实现不一致，验收时无法勾选 |
| D2 | 矩阵第 3 行返回目标写 `/dearseed`，实测为 `/` | 同上 |
| D3 | 必交证据缺提交号 | 证据门槛未闭合，阻塞 `User Review` |

### P2（记录即可）

| 编号 | 偏差 | 影响 |
| --- | --- | --- |
| D4 | 目标第 3 条「主壳头像仍指向 Profile 页」描述失真（实际 `/membership`） | 文档误导，非实现问题 |
| D5 | 全卡无 Mockplus 节点引用 | 违反事实门槛；若 T046 系现场反馈无原型节点，应显式标注 |
| D6 | **鉴权不一致**：`/` 有登录守卫，`/dearseed` 与 `/dearseed/membership` 无守卫。未登录用户可绕过首页守卫、经 `/dearseed` 直达会员中心 | 路由准入策略不统一，建议单开卡 |
| D7 | `routes.ts` 中 `/dearseed/membership` 的 `entry` 字段声称三个入口（首页头像 / 中部会员卡 / 快捷入口「会员空间」），但后两者位于 `/dearseed` 而非「专栏首页」，混淆了两个页面 | 路由档案失真 |

---

## 4. 流程性问题（需你裁决）

**本轮施工**改写了 PRD 卡片自身的 `## 产出` 段落：

```diff
-- `src/pages/dearseed/DearseedHome.tsx` 或等价文件：头像跳转 handler 修正。
-- 若路由 slug 与 T049 同步调整，需要联动更新 `src/app/router.tsx`。
+- `src/pages/Home.tsx`：右上角头像跳转 handler 修正（见下）。
+- 路由 slug `/dearseed/membership` 早已在 `src/app/router/routes.ts` 注册……本卡无需改路由。
```

两个问题：

1. **验收依据被事后对齐。** 规范做法是保留 PRD 条款原文，另起「实现偏差说明」记录差异；直接替换会让审计链断裂——事后无法分辨「原本要求什么」和「实际做了什么」。
2. **原卡的笔误被静默删除而非更正。** 原 `## 产出` 第 2 条把 slug 联动对象写成 `T049`，实际应为 `T051`（T049 是「洗头搭子泡泡值」决策卡）。另外原卡引用的 `src/pages/dearseed/DearseedHome.tsx` 路径不存在，`src/pages/dearseed/` 目录也不存在。

**需要说明的是**：`## 目标`、`## 验收标准`、`## 状态与交互矩阵`、`## 必交证据` 四节均**未被改写**，验收基准没有被事后对齐——这也是为什么上述 D1/D2/D3 能被查出来。

---

## 5. 建议动作

| # | 动作 | 归属 | 执行情况（2026-09-11） |
| --- | --- | --- | --- |
| 1 | 恢复 T046 卡 `## 产出` 原文，另起「实现偏差说明」记录实际改动 | 本卡 | ✅ 已执行（R4） |
| 2 | 更正条款笔误：`T049` → `T051`；补注 `DearseedHome.tsx` 路径不存在 | 本卡 | ✅ 已执行（R4、R7 — 笔误在本卡出现 2 次） |
| 3 | 修订矩阵第 2 行（标注「因 T037 守卫不可达」）与第 3 行（`/dearseed` → 返回上一页） | 本卡 | ✅ 已执行（R2、R3） |
| 4 | 更正目标第 3 条为「仍指向 `/membership`」 | 本卡 | ✅ 已执行（R1） |
| 5 | 补 Mockplus 节点引用，或标注「现场反馈，无原型节点」 | 本卡 | ✅ 已执行（R5） |
| 6 | 提交后回填提交号，再推进 `User Review` | 本卡 | ⏳ 待提交（需用户确认 commit/push） |
| 7 | 单开卡处理 D6（`/` 与 `/dearseed` 鉴权不一致） | 新卡 | ⏳ 已登记在卡内「已知偏差 6」+ README 台账待办 |
| 8 | 修正 `routes.ts` 中 `/dearseed/membership` 的 `entry` 字段描述 | 本卡或 T051 | ⏳ 未执行（属 T051 范围，避免跨卡改写） |

检查中额外发现（未列入原建议）：

| # | 发现 | 处理 |
| --- | --- | --- |
| 9 | `## 当前事实与差距` 第 1 条把两页旧目标混为一谈（`/` 原跳 `/mall`、`/dearseed` 原跳 `/profile`） | ✅ 已加更正注（R6） |
| 10 | T043 身份选择弹窗在 `/` **和** `/dearseed` 上都会弹出并遮挡头像（原卡只写了首页） | ✅ 已补注卡内「已知偏差 3」 |
| 11 | `T050-dearseed-membership-birthday-3-month.md:12` 写「详见 T049 联动」，疑为同类笔误（T049 = 洗头搭子泡泡值，T051 = 会员中心改名） | ⏳ **未擅改**——属他卡范围且存在两种解释，已登记待确认 |

**修订后卡片状态**：`Agent Review` 维持不变。`## 验收标准` 5 条实测全通过，但「对应提交号」未回填，**仍不具备进入 `User Review` 的条件**。

**本次检查对建卡规范的沉淀**：卡片条款修订一律采用「原文 → 修订后 → 依据」三列表（见卡内 `## 条款修订记录` R1–R7），不直接替换原文，以保留「原本要求什么 vs 实际做了什么」的审计链。

---

## 6. 附：检查用脚本

`scripts/verify-t046-prd.mjs` —— 不复用 `capture-t046.mjs` 的断言，独立按 PRD 条款取事实。

```bash
export PATH=/Users/hin/.workbuddy/binaries/node/versions/22.22.2-2/bin:$PATH
BASE_URL=http://127.0.0.1:5183 node scripts/verify-t046-prd.mjs
```

8 项检查实测输出：

```text
[符合]          C1 未登录访问 /       :: 落点 /legacy-profile/login；from 参数=(无)
[符合]          C0 登录后进入 /       :: 落点 /
[符合]          C2 头像测试锚点       :: [data-dearseed-avatar] 数量=1，可见=true
[符合]          C3 商城入口残留       :: aria-label="进入卡博士商城" 命中 0 处
[符合]          C4 头像跳转目标       :: / → /dearseed/membership；标题栏 ≈"会员中心"
[与 PRD 字面不符] C5 会员中心返回落点  :: 返回落到 /
[与 PRD 字面不符] C6 主壳头像基线      :: 点击后 → /membership
[符合]          C7 控制台/资源错误    :: 无
```
