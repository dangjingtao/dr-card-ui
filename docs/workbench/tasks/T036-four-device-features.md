# T036｜四大设备功能页面（淋浴/洗烘/饮水/吹风）

## 状态与类型

- 状态：`Done`

- 类型：UI / Feature / Device

- 优先级：P0

## 当前事实与差距

- 四大设备功能（淋浴/洗烘/饮水/吹风）是卡博士APP的核心服务，通过首页四金刚区入口进入。

- 每个设备类型有独立的主题色：淋浴（紫色）、洗烘（青绿）、饮水（蓝色）、吹风（暖黄）。

- 设备动线：首页金刚区 → 设备列表页 → 扫码/点击使用 → 设备详情页（金额选择 → 启动 → 结算）。

- 已完成设备列表页、设备详情页（含启动流程、结算、紧急停止、保修悬浮球）的全部施工。

## 目标

1. 完成淋浴/洗烘/饮水/吹风四类设备的完整页面施工。
2. 统一设备页面的交互模式，通过主题色区分设备类型。
3. 打通首页 → 设备列表 → 设备详情 → 结算的完整动线。

## 原型范围

- 设备列表页（`/device/:type`）：设备图标+名称+位置+编号+状态+扫码按钮。

- 设备详情页（`/device/connecting`）：设备信息、账户余额、金额选择、四步启动状态、确定启动/结算按钮、紧急停止、保修悬浮球。

- 结算弹窗：设备已停止 + 本次消费 + 剩余余额 + 我知道了。

- 紧急停止弹窗：设备已强制停止 + 本次消费 + 剩余余额 + 我知道了。

## 不在范围

- 不实现真实扫码功能（点击扫码按钮直接进入详情页）。

- 不接入真实余额数据（使用 fixture 模拟）。

- 保修页面暂不施工，仅保留悬浮球入口。

- 不实现设备使用中的实时计时/扣费动画。

## 依赖与阻塞决策

| 编号    | 阻塞项      | 风险 | 说明                                |
| ----- | -------- | -- | --------------------------------- |
| B-052 | 金额档位设定   | 低  | 当前为 1/3/5/10/20 元，需产品确认实际档位       |
| B-053 | 启动步骤文案   | 低  | 四步文案（设备连接成功/下发指令/查询状态/启动成功）为参考图内容 |
| B-054 | 保修页面入口形态 | 低  | 悬浮球位置与样式待产品确认                     |

## 实施要求

- 四种设备共用同一套页面组件，通过 `:type` 参数切换主题色与文案。

- 设备列表页卡片结构：左侧图标 + 中间名称/位置/编号 + 右侧状态/按钮。

- 设备详情页包含：设备信息卡、账户余额、金额选择、启动状态列表（四步）、底部操作按钮、紧急停止、保修悬浮球。

- 启动流程：四步逐步点亮动画，完成后按钮变为"结算"。

- 结算与紧急停止均弹出对应弹窗，点击"我知道了"关闭并扣除余额。

- 所有页面宽度不超过 480px，居中显示。

## 状态与交互矩阵

- 设备列表：空闲态（扫码按钮）/ 使用中（灰色标签）。

- 设备详情 idle 态：金额可选，按钮为"确定启动"，紧急停止禁用。

- 设备详情 starting 态：金额不可选，按钮为"启动中..."，启动步骤逐步点亮。

- 设备详情 running 态：按钮为"结算"，紧急停止可用，四步全亮。

- 结算弹窗：显示消费金额与剩余余额，点击"我知道了"回到 idle 态。

- 紧急停止弹窗：红色风格，点击"我知道了"回到 idle 态。

## 验收标准

1. 四种设备页面主题色正确，与首页金刚区图标颜色对应。
2. 设备列表页卡片信息完整（图标/名称/位置/编号/状态）。
3. 设备详情页启动流程动画正常（四步依次点亮）。
4. 结算流程：启动成功 → 结算 → 弹窗 → 余额扣除 → 回到初始态。
5. 紧急停止：启动中/运行中可点击 → 弹窗 → 余额扣除 → 回到初始态。
6. `npm run typecheck` 和 `npm run build` 通过。

## 必交证据

- 四种设备列表页截图（各一张）。

- 设备详情页三态截图（idle / starting / running）。

- 结算弹窗截图。

- 紧急停止弹窗截图。

- 完整动线验证：首页 → 淋浴列表 → 详情 → 启动 → 结算 → 返回。

- 工程检查：typecheck / build 通过。

## 产出

- 新增 `src/pages/DeviceListPage.tsx`：设备列表页（四套主题色复用）。

- 新增 `src/pages/DeviceDetailPage.tsx`：设备详情页（金额选择+启动流程+结算+紧急停止）。

- 新增 `src/app/fixtures/device.ts`：设备数据与主题配置。

- 新增 `src/styles/card-doctor-theme.css`：设备主题 CSS 变量。

- 更新 `src/app/router/routes.ts` 与 `src/app/router/index.tsx`：设备路由配置。

## 迭代（2026-09-07 列表按钮文案 + T030 联动）

- 背景：用户调整"附近设备列表"的交互——列表里每台设备直接显示启动按钮，用户选中设备就启动，不再需要先点"扫码使用"再进扫码页；底部的「立即扫码」按钮保留，作为扫码启动的备选入口。启动流程本身不变。

- 改动（`src/app/fixtures/device.ts` 的 `DEVICE_THEMES`）：

  - 淋浴 / 洗烘 / 吹风：卡片按钮文案 `扫码使用` → `启动`
  - 饮水：卡片按钮文案 `扫码取水` → `取水`
  - 饮水：底部扫码按钮文案 `扫码取水` → `立即扫码`（与其它设备一致）
  - 底部「立即扫码」按钮保留，跳 `/legacy-home/scan?device=...` 不变
  - 启动动线完全不变：点卡片按钮仍跳 `/device/connecting?type=...&id=...`，T030 的自动启动逻辑照常生效

- 工程门：`npm run typecheck` ✅ 通过。

- 提交：`dc62e98 设备列表页卡片按钮：扫码使用 → 启动/取水，底部立即扫码按钮保留，启动流程不变`。

- 状态影响：T036 原 `Done` 状态不变，本次为文案与交互入口微调，不改变验收结论。

## 迭代（T036R1｜2026-09-08 设备列表不再区分使用中）

- 背景：与卡博士运营负责人 2026-09-08 沟通确认 —— 卡博士的硬件设备不一定能回传使用状态，设备列表页继续展示「使用中」徽标/灰态会让用户产生误判（被锁住一台本可扫码启动的淋浴）。决定：列表卡片不再展示使用状态，全部统一展示「扫码启动/扫码取水」按钮。
- 改动：
  - `src/pages/DeviceListPage.tsx`：
    - 删除卡片名右侧的「空闲 / 使用中 / 离线」三态徽标（`bg-green-100 / bg-orange-100 / bg-gray-100`）。
    - 卡片右侧按钮不再根据 `device.status` 三态分流：一律展示「扫码启动」（淋浴/洗烘/吹风）/「扫码取水」（饮水）按钮，点击即弹扫码 BottomSheet。
  - `src/app/fixtures/device.ts`：淋浴 `shower-002` 原先标 `in-use`，本次与新策略对齐改为 `idle`；其余设备保持 `idle`。
  - `DeviceInfo.status` 类型 `'idle' | 'in-use' | 'offline'` 仍保留以兼容未来接口（不再在列表渲染中消费）。
- 工程门：`npm run typecheck` ✅ 通过；`npm run build` ✅ 通过（1.47s）。
- 影响范围：仅设备列表卡片视觉 + fixture；不影响 `DeviceDetailPage` 启动流程与四步动画、不影响顶部固定扫码按钮、不影响底部扫码启动 BottomSheet。
- 与 T030 的边界：T030 负责「设备详情页启动流程」（淋浴/饮水自动启动等），T036R1 负责「设备列表页 UI」；两者解耦，T036R1 不动 `DeviceDetailPage`。
- 任务编号：**T036R1**（T036 的 1 号增量任务卡）。
- 状态影响：T036 原 `Done` 状态不变，本次为列表视觉与交互调整，不改变 T036 原有验收结论，也不影响 T030 的设备详情启动流程验收。

## 迭代（T036R2｜2026-09-08 设备列表「账户余额」改为只读 pill）

- 背景：用户 2026-09-08 反馈，设备列表右上角的「账户余额」按钮点击会进入"我的小票"/学校账户列表页，离查看余额的诉求多了一层跳转；调整为直接在 pill 上展示余额，免跳转，方便用户快速看到当前余额。
- 改动（`src/pages/DeviceListPage.tsx`）：
  - 顶部栏右侧原 `<button>` 跳 `/legacy-profile/school-accounts` 改为只读 `<div>` pill，保留半透明白底 + 白字 pill 视觉。
  - 从 `useUserInfo()` 读取 `balance`（`userInfoStore.ts` 中的 mock 余额字段，初始 100.00），格式化为 `¥ 100.00` 直接渲染在 pill 上：`余额 ¥ 100.00`。
  - 移除 `onClick`、`active:bg-white/30` 等可点击态样式；保留 `aria-label` 让无障碍读屏仍能读出"账户余额 ¥ 100.00"。
  - 顶部栏左侧的「返回」按钮、标题居中、底部「立即扫码」按钮等均不变。
- 工程门：`npm run typecheck` ✅ 通过；`npm run build` ✅ 通过（1.50s）。
- 边界澄清：仅作用于 `DeviceListPage`，不动 `DeviceDetailPage`、不动 `userInfoStore.balance` 的写入语义、不动 T036 原有的四设备主题色与广告位策略。
- 任务编号：**T036R2**（T036 的 2 号增量任务卡）。
- 状态影响：T036 原 `Done` 状态不变，本次为顶部栏 pill 文案与跳转行为调整，不改变 T036 原有验收结论，也不影响 T030 设备详情启动流程与 T036R1 列表"使用中"处理。

