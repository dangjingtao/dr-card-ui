# T024｜2026-08-27 UI 变更验收

## 状态与类型

- 状态：`User Review`（2026-08-27 验收执行完毕，37/37 通过；智能体不写入 `Accepted`，等待用户确认）
- 类型：Test / Review / Change Request
- 优先级：P0

## 当前事实与差距

- T015 已验收的是 2026-08-24 的旧页面基线，不能替代本轮新增需求验收。
- ~~T021–T023 尚未提交新的视觉、状态、交互和回归证据。~~ 已解除：T021 `Agent Review`、T022 `Accepted`、T023 `Accepted`，三张卡的取证脚本均已复跑通过。

## 目标

独立验收 2026-08-27 UI 变更批次，并确认旧有未涉及链路没有发生回归。

## 原型范围

- 需求文档：[`2026-08-27-ui-change-requirements.md`](../../requirements/2026-08-27-ui-change-requirements.md) 全部条目。
- T021–T023 的所有页面、状态、入口与返回路径。

## 不在范围

- 不在验收卡中补做页面或修改需求。
- 不重新验收与本轮无关的全部 72 个原型节点。
- 不因构建通过自动判定 UI 通过。

## 依赖与阻塞决策

- 依赖 T021–T023 达到 `Agent Review`。
- 任一施工卡存在阻塞或未覆盖状态时，本卡不能进入 `User Review`。

## 实施要求

- 建立本轮页面、状态、交互和返回路径矩阵。
- 在 375 × 812 下完成视觉截图与主链路点击验证。
- 抽查窄屏、长页滚动、固定底部导航、弹窗遮罩和安全区。
- 回归首页导航、体验券核销、泡泡值筛选及商城边界。
- 运行类型检查、生产构建、直达刷新与控制台检查。

## 状态与交互矩阵

- T021：新首页、新人 1/2 张体验券、领取成功、关闭、打卡内容。
- T022：任务页、明细四态、三福利入口、兑换按钮。
- T023：核销两态、商品区跳转、会员中心跳转、商城边界三态。
- 跨卡链路：首页领券 → 体验券；泡泡值 → 明细；体验券/会员中心 → 商城。

## 验收标准

- 需求文档中的每条 UI 要求均有截图或交互证据。
- 无死链、假按钮、重复页面壳、横向溢出和阻塞性控制台错误。
- 随机 1/2 张体验券可通过确定性验收入口分别复现。
- `npm run typecheck`、`npm run build` 和生产预览 smoke test 通过。
- 智能体最多将本卡推进到 `User Review`，仅用户可标记 `Accepted`。

## 必交证据

- 本轮验收矩阵与截图索引。
- 三条跨卡主链路操作记录。
- 工程命令、控制台、直达刷新与响应式检查结果。
- 已知差异、失败项和回退卡片清单。

## 产出

- 2026-08-27 UI 变更验收报告、截图与用户验收记录。

## 验收执行记录（2026-08-27）

- 验收报告：[`t024-acceptance-report.md`](../t024-acceptance-report.md)
- 验收脚本：[`verify-t024.mjs`](../../../scripts/verify-t024.mjs)
- 机读结果：[`evidence/t024-results.json`](../evidence/t024-results.json)，`total 37 / passed 37 / failed 0`
- 截图：`evidence/screenshots/t024-01…t024-13` 共 14 张（含 `coupon-1` / `coupon-2` 两张券态）
- 运行环境：Node.js 22.23.2 + Chromium + Vite production preview（4173 被占用，实跑 `http://127.0.0.1:4174`）

逐条对应：

- 需求 §7 十一条 UI 要点 → 检查项 `R7-01`…`R7-11b`，全部 PASS。
- 三条跨卡主链路 → `L1-01` 首页领券 → `/exchange`；`L2-01` 泡泡值 → `/points/detail`；`L3-01` 商品信息 → `/mall` 且可返回 `/card`。
- 抽查项 → 长页固定导航 `R2-01`、横向溢出 `R2-02`、二级页壳层 `R2-03`、弹窗遮罩 `R3-01`、底部安全区 `R3-02`、直达刷新 `R4-01`（6 条路径）、窄屏 320×480 `R5-01`（4 条路径）、商城三态与重试 `R6-*`。
- 工程命令 → `npm run build`（含 `typecheck` 与 `verify:images`）exit 0；`capture-t021/t022/t023.mjs` 全部通过；`verify-t015.mjs` 为 `58/60`，与既有基线一致，无回归。
- 控制台 → 无 `console error` / `pageerror`；仅两类 React Router v7 future flag 提示，列为已知框架噪音。

已知差异与失败项：

- 范围外历史差异：`verify-t015 #18 /exchange`、`#19 /profile` 缺台账标志文案，非本轮变更引入，按「不在验收卡中补做页面或修改需求」保持不动。
- 过程事件：`verify-t015 #62` 首跑失败一次（整壳未渲染 + 一条 404），探针同路径连跑 5 次全部正常、整体重跑回到 58/60，判定为预览服务一次性加载抖动，非回归。
- open 阻塞 B-031 / B-032 / B-033 均属接口阶段收口项，其已定案部分已实现并取证，不阻塞本轮 UI 判定。
- 环境限制：headless 下 `env(safe-area-inset-bottom)` 为 `0px`，真机刘海设备的底部面板留白建议人工复核一次。

按验收标准「智能体最多将本卡推进到 `User Review`，仅用户可标记 `Accepted`」，本卡停在 `User Review`。
