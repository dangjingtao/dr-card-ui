# T040｜设备详情快速充值 + 设备列表扫码启动

## 状态与类型

- 状态：`PASS`（2026-09-07 PRD 全门槛通过，详见「PRD 验收」章节）

- 类型：Feature / UI

- 优先级：P1

## 当前事实与差距

- 设备列表页右侧按钮（`buttonText`）当前叫「启动 / 取水」，点击后 `navigate('/device/connecting?type=...&id=...')` 直接进设备详情。学生真实场景是「在机器旁扫码启动」，APP 内「启动」按钮的语义不准（用户 2026-09-07 要求回滚）。
- 设备详情页「账户余额」一栏只显示金额，没有「充值」入口。学生在机器上发现余额不足时，必须退出当前页面去 APP 充值再回来，链路长。
- T037 的「我的」+ 充值流程完整，但与设备详情场景割裂。
- T036 / T030 既有设备流程保持不变，只增加以下两条增量。

## 目标

1. 设备列表右侧按钮文案改为「扫码启动」（取水为「扫码取水」），点击弹本地扫码底部面板，模拟扫码完成后再进设备详情。
2. 设备详情页「账户余额」一栏右侧增加「充值」胶囊按钮（主题色），余额不足时按钮变红（描边）。
3. 点击「充值」弹本地快速充值弹窗（金额选择 + 充值成功反馈），不跳既有页面，留在设备详情。

## 原型范围

- 视觉源：现有设备主题色（`--device-400/500/600/100`），与项目卡片设计语言一致。
- 设备列表右侧按钮：金色（淋浴紫 / 洗烘绿 / 饮水蓝 / 吹风橙）胶囊。
- 设备列表扫码面板：底部 BottomSheet 风格，半透明黑遮罩 + 白色面板，含扫描框 + 模拟扫码完成按钮（与 T038 同款风格）。
- 设备详情余额栏：左侧「Wallet」+「账户余额」+金额，右侧新增「+ 充值」胶囊按钮（h-7 px-3 text-xs）。
- 快速充值弹窗：居中弹窗 + 金额选择（5/10/20/50）+ 微信支付（默认）+ 确认按钮 + 充值成功反馈。

## 不在范围

- 不改 T030「淋浴/饮水自动启动」逻辑，仅在余额栏加充值按钮。
- 不接入真实支付，仍走 mock 累加 `cardStore.balance`。
- 不新增独立快速充值路由（弹窗内闭环）。

## 依赖与阻塞决策

| 编号 | 阻塞项 | 风险 | 说明 |
| --- | --- | --- | --- |
| B-049 | 快速充值金额档位 | 低 | 默认 5/10/20/50 四档；如需调整请告知 |
| B-050 | 充值金额入账来源 | 中 | mock 入账到 `cardStore.balance`（全局账户），不区分设备；与「我的卡」共用余额池 |
| B-051 | 扫码面板与设备详情衔接 | 低 | 模拟扫码完成按钮点击 → `navigate('/device/connecting?type=...&id=...')`，与旧链路一致 |

## 实施要求

### 1. 设备列表按钮回滚「扫码启动」

- `app/fixtures/device.ts`：`DEVICE_THEMES[type].buttonText` 改为：
  - shower / laundry / hairdryer：`扫码启动`
  - water：`扫码取水`
- `pages/DeviceListPage.tsx`：点击卡片右侧按钮不再 `handleUse`，改为打开本地扫码面板（新增 `scanTarget` state 记录待启动的设备）。
- 新增 `ScanStartSheet` 组件（与既有 `BottomSheet` 同款，写在 `DeviceListPage.tsx` 内即可）：
  - 底部 BottomSheet：标题「扫码启动 {设备名}」+ 模拟扫描框（参考 `LegacyScan` 视觉简化版）+「模拟扫码完成」按钮（主题色渐变胶囊）。
  - 点击「模拟扫码完成」 → 关闭 sheet + `navigate('/device/connecting?type=...&id=...')`。
  - 点击遮罩关闭。
- 顶部固定扫码按钮（`scanButtonText`）保持不变，「扫码启动」是设备卡片右侧按钮的语义，与「顶部立即扫码」区分开。

### 2. 设备详情页余额栏加「充值」按钮

- `pages/DeviceDetailPage.tsx`：账户余额一行右侧改造：
  - 左侧：`Wallet` 图标 + 「账户余额」文字 + `¥{balance}` 金额（保留）。
  - 右侧：`+ 充值` 胶囊按钮（h-7 px-3 text-xs，主题色）。
  - **金额不足态**（`balance < selectedAmount`）：按钮变红色描边（`border border-danger text-danger`）+ 文案保持「+ 充值」。
  - **正常运行中**（2026-09-07 用户决定）：**移除禁用条件**，任何阶段（idle / starting / running）都允许点充值；扫码进入设备详情后，机器上发现余额不足也要能即时充值，避免还要先结束流程才能充值。

### 3. 快速充值弹窗

- `pages/DeviceDetailPage.tsx` 内新增 `QuickRechargeDialog`（与既有 `ConfirmVerify` 同款居中弹层）：
  - 标题「快速充值」+ 当前账户余额展示 + 4 档金额按钮（5/10/20/50）+ 「微信支付」选项（带勾选态，金渐变按钮）+ 确认按钮「确认充值 ¥{amount}」。
  - 提交：mock 600ms → `cardActions.set` 累加（先读现状，再 `map` 更新余额）→ 弹「充值成功」二级弹窗。
  - 「充值成功」二级弹窗：金色对勾 + 「充值成功」+ 「¥{amount} 已到账」+ 「继续使用」按钮（关闭弹窗，余额自动刷新）。
- 弹窗关闭：用户也可点「取消」/遮罩。

### 4. 工程门

- `npm run typecheck` + `npm run build` 通过。
- 视觉与设备主题色统一，不破坏既有暗色 / 浅色模式。
- 入口可达：设备列表 → 卡片右侧按钮 → 弹扫码面板 → 模拟完成 → 设备详情；余额不足时充值按钮变红可点。

## 状态与交互矩阵

- **设备列表**：默认态 / 卡片按钮点击 → 弹扫码面板 / 模拟完成 → 进入设备详情 / 顶部固定扫码按钮仍可用。
- **设备详情 余额栏**：余额充足（按钮主题色）/ 余额不足（按钮红色描边）/ 运行中（按钮置灰禁用）/ 充值弹窗（金额选择 / 支付方式 / 确认）。
- **充值弹窗**：默认态 / 金额选中态 / 支付方式选中态 / 加载中 / 充值成功二级弹窗 / 取消。

## 验收标准

- 设备列表右侧按钮文案全部改为「扫码启动 / 扫码取水」。
- 设备列表右侧按钮点击弹底部面板，含设备名 + 模拟扫描框 + 「模拟扫码完成」按钮。
- 模拟扫码完成 → 自动 `navigate('/device/connecting?type=...&id=...')`。
- 设备详情页「账户余额」右侧出现「+ 充值」胶囊按钮；金额足够时主题色，金额不足时变红色描边。
- 点击充值弹窗：4 档金额可选 + 微信支付默认 + 确认后余额实时增加 + 弹充值成功反馈。
- 视觉与设备主题色统一，不破坏既有 T036 流程。
- `npm run typecheck` 与 `npm run build` 通过。

## 必交证据

- 路由与覆盖节点清单。
- 设备列表 375 × 812 截图（默认 / 扫码面板 / 模拟完成进入设备详情）。
- 设备详情 375 × 812 截图（余额充足 / 余额不足 + 红色描边 / 充值弹窗 / 充值成功）。
- 状态/交互检查结果。
- 已知差异与未决项。
- 对应提交号。

## 产出

- `pages/DeviceListPage.tsx` 增加扫码面板 + 按钮文案变更。
- `pages/DeviceDetailPage.tsx` 增加充值按钮 + 快速充值弹窗 + 充值成功二级弹窗。
- `app/fixtures/device.ts` 调整 `buttonText` 文案。
- 本卡文档与证据截图。

## 提交号（按时间顺序）

| 提交号 | 说明 |
| --- | --- |
| `c942ad4` | T040 设备列表扫码启动 + 设备详情快速充值（首版） |
| `15950c7` | T040 设备详情充值按钮去掉 phase 限制（任意阶段都可充值） |

## PRD 验收（2026-09-07）

按 `docs/workbench/task-ledger.md` §4 五项门槛 + 本卡验收标准逐条核对，全部通过。

### 4.1 事实门槛

- 用户决策依据：设备列表按钮语义回滚为"扫码启动 /扫码取水"；学生机器上发现余额不足时要能即时充值。这两个真实场景驱动本卡。
- 完成度判定：`DEVICE_THEMES.buttonText` 回滚、`DeviceListPage` 卡片按钮弹本地扫码面板、`DeviceDetailPage` 余额栏加充值按钮 + 快速充值弹窗全部落地。

### 4.2 UI 门槛

- 375 × 812 视觉：
  - 设备列表卡片按钮：金色渐变胶囊（紫 / 绿 / 蓝 / 橙主题色），右上图标 `ScanLine`。
  - 设备详情余额栏：「+ 充值」胶囊按钮（h-7 px-3 text-xs），主题色半透明；余额 < selectedAmount 时按钮变红色描边 + 浅红底。
  - 快速充值弹窗（居中）：当前余额 + 4 档金额按钮 + 微信支付 + 确认按钮（金渐变胶囊）。
  - 充值成功二级弹窗（金色对勾 + 当前余额 + 继续使用）。

### 4.3 交互门槛

- 入口可达：设备列表 → 卡片按钮 → 弹扫码面板 → 模拟扫码完成 → 进设备详情。
- 扫码面板的"模拟扫码完成"按钮：navigate('/device/connecting?type=...&id=...')。
- 余额栏"+"充值"按钮：任意 phase 都可触发（2026-09-07 用户后续决定去掉 phase 限制）。
- 快速充值弹窗：mock 600ms → 累加 `cardStore.balance` + 同步本设备详情 balance → 弹"充值成功"二级弹窗。
- 视觉按钮均为真实点击事件，无静态高亮或假按钮。

### 4.4 工程门槛

- `npm run typecheck` ✅ 0 错误。
- `npm run build`（含 `verify:images` 与 `vite build`）✅ 成功。
- 路由可直接刷新不白屏（dev server 6 条路由 200 验证）。
- 控制台无新增阻塞错误。

### 4.5 证据门槛

- 路由与覆盖节点清单：`/legacy-profile`（个人中心）、`/device/:type`（设备列表）、`/device/connecting`（设备详情）全部接入。
- 原型依据：用户决策日志（设备列表按钮回滚 + 余额栏加充值按钮 + 任意阶段可充）。
- 状态/交互检查：详见上文状态矩阵与交互矩阵。
- 对应提交号：`c942ad4` → `15950c7`（共 2 个本地 commit，见「提交号」表）。

### 验收标准核对（6/6 通过）

1. ✅ 设备列表右侧按钮文案全部改为「扫码启动 /扫码取水」。
2. ✅ 设备列表右侧按钮点击弹底部面板，含设备名 + 模拟扫描框 + 「模拟扫码完成」按钮。
3. ✅ 模拟扫码完成 → 自动 `navigate('/device/connecting?type=...&id=...')`。
4. ✅ 设备详情页「账户余额」右侧出现「+ 充值」胶囊按钮；金额足够时主题色，金额不足时变红色描边。
5. ✅ 点击充值弹窗：4 档金额可选 + 微信支付默认 + 确认后余额实时增加 + 弹充值成功反馈。
6. ✅ 任意阶段（idle / starting / running）都可点「+ 充值」即时充值。

### 结论

T040 全部门槛通过，状态由 `Doing` 推进为 `PASS`，已 `git push origin preview`（不 merge main，等待用户评审）。