# T051｜诗得丽专栏「我的」页改名「会员中心」

## 状态与类型

- 状态：`Ready`
- 类型：Rename / IA（页面级文案与路由改造）
- 优先级：P0（品牌专栏 IA 调整）
- 所属阶段：诗得丽品牌专栏施工（2026-09-09 批次）

## 当前事实与差距

- 现状：诗得丽专栏内个人页面叫「我的」。
- 需求：改名为「会员中心」。
- 关联项：T046 的头像跳转目标也要从"专栏「我的」"统一为「会员中心」。

## 目标

1. 诗得丽专栏内的个人页面在所有入口/标题/breadcrumb 中显示为「会员中心」。
2. 同步调整路由 slug 为 `/dearseed/membership`（与 T046 一致）。
3. 旧路由 `/dearseed/me` 或 `/dearseed/profile` 在过渡期做 301/重定向到新路由，避免深链失效。
4. 不影响卡博士 APP 主壳的「我的」页（保留原名）。

## 原型范围

- 需求来源：2026-09-09 用户现场反馈。
- 节点：诗得丽专栏 → 个人页（标题、Tab 文案、底部 Tab 文字、跳转入口文案）。

## 不在范围

- 不修改卡博士 APP 主壳「我的」页文案。
- 不重做会员中心页面布局，仅做改名 + 路由迁移。
- 不修改会员中心内的功能模块（如打卡、搭子、卡券等），这些由其他任务卡覆盖。

## 依赖与阻塞决策

| 编号    | 阻塞项            | 风险 | 说明                                            |
| ----- | -------------- | -- | --------------------------------------------- |
| B-052 | 路由 slug 是否保留旧 `me` 作为重定向 | 低  | 建议保留 `/dearseed/me` → `/dearseed/membership` 重定向 |
| B-053 | 底部 Tab 文案同步     | 低  | 底部 Tab「我的」也改为「会员中心」；或保留图标 + 改文字，保持视觉一致    |

## 实施要求

- 标题文案：所有「我的」个人页面标题改为「会员中心」。
- 路由：
  - 新路由：`/dearseed/membership`。
  - 旧路由重定向：`/dearseed/me` 与 `/dearseed/profile` → `/dearseed/membership`。
- 入口跳转：
  - 专栏首页头像（T046 已联动）跳 `/dearseed/membership`。
  - 任何底部 Tab / 设置入口 / 通知深链指向旧路由的，全部更新。
- 与 SEO / 分享链接相关的元信息同步更新（title、description）。
- 不影响会员中心内部子页面（搭子详情、卡券详情、通知等）。

## 状态与交互矩阵

| 状态      | 入口                | 跳转目标                       |
| ------- | ----------------- | -------------------------- |
| 旧路由直访   | `/dearseed/me`    | 重定向至 `/dearseed/membership` |
| 旧路由直访   | `/dearseed/profile` | 重定向至 `/dearseed/membership` |
| 新路由直访   | `/dearseed/membership` | 直接展示「会员中心」                |
| 专栏首页头像  | 头像按钮              | `/dearseed/membership`       |
| 专栏首页底部 Tab | 「我的」/「会员中心」       | `/dearseed/membership`       |

## 验收标准

- 所有可见文案为「会员中心」，没有「我的」残留（卡博士主壳除外）。
- 旧路由访问自动重定向到新路由。
- 头像点击进入新页面。
- `npm run typecheck` / `npm run build` 通过。
- 375×812 实现截图与说明对照。

## 必交证据

- 会员中心 375×812 截图（含头部标题）。
- 旧路由 → 新路由跳转验证截图。
- 文字 diff 记录（旧 → 新）。
- 对应提交号。

## 产出

- `src/pages/dearseed/MembershipCenter.tsx`（或等价文件，从 `MyPage.tsx` 改名）。
- `src/app/router.tsx`：路由配置 + 重定向。
- `src/app/fixtures/profile.ts`（如适用）：标题与导航文案。