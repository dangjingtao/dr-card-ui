# 决策记录｜T009 卡包三态、分享与兑换码失败态

> 决策日期：2026-08-22
> 决策卡：[T009-wallet-redemption.md](../tasks/T009-wallet-redemption.md)
> 决策类型：UI / Flow
> 决策范围：Mockplus 节点 #54、#62、#63、#64、#65、#66、#67、#68、#69
> 决策人：智能体按 `AGENTS.md` §11 证据阶梯裁定，未新增产品规则；B-015 / B-016 / B-017 由用户定案关闭
> 状态：Accepted（三个阻塞项已由用户关闭，见 §7）

## 1. 决策目的

T009 施工中出现两类需要落账的问题：

- 卡包结构上，`docs/prototype/04-mall-card-order.md` §8 与 `reference/卡包.html` 不一致；
- 分享（#65/#66）与兑换失败态（#68）涉及尚未确认的产品规则，不能靠推断补齐。

本记录固定本轮采用的依据；曾挂起的三项（B-015 / B-016 / B-017）已由用户定案关闭并写入 §7，其余未确认部分仍显式挂在 §8，避免后续把夹具当成产品规则。

## 2. 决策结论

| 编号 | 决策 | 依据 | 关联节点 |
| --- | --- | --- | --- |
| D-014 | 卡包沿用 **可用 / 已使用 / 已过期 三态 Tab**，不引入原型文档写的第 4 个「全部」Tab，也不加右上角「使用记录」入口 | D-008 已接受 `reference/卡包.html` 为该页事实源，其分段控件只有三项（计数 2/0/1）且无「使用记录」；T009 卡第 11 行与验收标准第 61 行同样写「三态」。**用户已关闭 B-016 并确认该口径**（卡包以 `reference/卡包.html` 为准，不加「全部」和「使用记录」） | #54 #62 #63 #64 |
| D-015 | `#63 卡包-已使用` **保持空态**，不为了填充画面新增 `status: 'used'` 卡券夹具 | 标准页该 Tab 计数即为 0，并配有专属空态文案「暂无已使用的体验券 / 已核销或已完成使用的体验券，会显示在这里」；凭空造一张已使用券会推翻标准页画好的空态 | #63 #67 |
| D-016 | 分享为 **单选 + 搜索 + 「下一步」→ 成功页** 的前端闭环；对象列表按原型做「搭子列表」夹具，不做对方接受、次数限制、时效限制和持久化，分享后原卡包状态不变 | T009 卡实施要求「转赠选择、确认、成功和取消路径完整」；**用户已关闭 B-015 并定案上述范围** | #65 #66 |
| D-017 | 兑换码规则集中在夹具 `REDEEM_CODE_RULE`（`length` / `pattern` / `hint` / `pending` / `blocker` / `source`），页面与组件不得硬编码位数 | T009 卡验收标准第 63 行「未决规则有显式配置和决策记录，不散落在组件中」；12 位取自 `reference/兑换卡券.html`（B-009 由 D-008 关闭），但 D-004 要求不擅自统一原型 8/11 位差异，故保留 `pending: true` | #68 #69 |
| D-018 | 核销链路收口为 **确认 → 结果反馈 Toast → 回 `/card?state=used`**，并补 `?state=repeat` 重复核销态 | T009 卡实施要求第 47 行「核销页包含凭证、防误触确认、已核销反馈和重复核销状态」 | #67 |
| D-019 | `/card/share` 路由标题统一为 **「分享」**，不做逐状态标题；卡包入口按钮保留原型的「转赠」 | 原稿两节点 app-bar 分别是「选择接收的人」「分享成功」，本项目一条路由一个标题；`reference/分享.html` 用「分享」，`reference/卡包.html` 按钮用「转赠」，两处各自照原型 | #65 #66 |
| D-020 | 用 `DebugPanel` 从 `routes.ts` 登记的 `states` / `overlays` 派生状态胶囊，只改 URL 不引入本地状态 | 用户要求「设置一个调试面板展示不同状态」；URL 已是唯一事实源，面板复用登记项即可零重复维护 | 全部 |
| D-021 | 调试面板 **仅在 `?debug=1` 时渲染**，正常页面完全不出现；面板切换状态时 `debug=1` 原样保留 | 用户反馈折叠胶囊遮挡底部操作区。改为 URL 显式开关后，正常页面与原型完全一致，验收时又能一键进入调试 | 全部 |

`B-009` 此前已由 D-008 关闭；`B-015`（分享范围）、`B-016`（卡包 Tab 结构）、`B-017`（发货语义）由用户本轮定案关闭，详见 §7。T009 已无未关闭阻塞项。

## 3. 决策细节

### 3.1 卡包结构冲突（D-014、D-015）

两份文档的差异点：

| 关注点 | `docs/prototype/04-mall-card-order.md` §8 | `reference/卡包.html`（D-008 事实源） | 本轮实现 |
| --- | --- | --- | --- |
| Tab | 全部 / 可用 / 已使用 / 已过期（4 项） | 可用 / 已使用 / 已过期（3 项，计数 2/0/1） | 3 项 |
| 右上角 | 「使用记录」→ 订单管理 | 无该入口 | 无 |
| 已使用态 | 「卡包 - 已使用」列表状态 | 设计好的空态 + 专属文案 | 空态 |

按 §11 阶梯，`reference/卡包.html` 属「已确认并实际使用的标准页 + 已验收实现」，且与 T009 任务卡自身表述一致，优先于原型文档的概述性描述，故不改实现。

**B-016 已关闭**：用户定案「卡包以 `reference/卡包.html` 为准，只保留可用 / 已使用 / 已过期三个 Tab，不加「全部」和「使用记录」」，与 D-014 一致，本轮实现无需改动。

副作用说明：`确认核销` 成功后跳 `/card?state=used` 会落到空态页。这是标准页设定的结果（无已使用券夹具），不是链路断裂；跳转本身由脚本断言覆盖。

### 3.2 分享（D-016、D-019；B-015 / B-017 已关闭）

- 对象列表来自 `SHARE_TARGET_FIXTURES`（4 位，含头像字），分组标题按原型写「搭子列表」，默认选中首位，支持按名称搜索过滤。
- 按用户提供的原型（`reference/分享.html` #65、`reference/分享成功.html` #66）还原：#65 搜索框 + 搭子列表单选 + 底部主按钮；#66 成功 hero + 副文案「已成功分享给好友，邀请他也来一起玩吧」+ `aria-label="分享的商品"` 商品卡 + 「查看我的卡包」/「返回」双按钮。
- 标题口径：#65/#66 原稿 app-bar 分别为「选择接收的人」「分享成功」，本项目一条路由一个标题，故 `routes.ts` 的 `/card/share` 标题统一为 **「分享」**（D-019），不做逐状态标题。卡包页入口按钮文案仍是原型的「转赠」（`reference/卡包.html`），`entry` 字段照实记录，不反向改原型。
- 「返回」回到选择页并保留 `?coupon=`；成功页由 `?state=success` 直达。
- **B-015 已关闭**：按原型做「搭子列表」夹具，单选对象 → 下一步 → 分享成功；不做对方接受、次数限制、时效限制和持久化，分享后原卡包状态暂不改变。当前实现即为此范围。
- **B-017 已关闭**：#66 商品卡的「洗发试用装 / 已发货 / 单次使用」仅作为原型展示夹具（`SHARE_PRODUCT_FIXTURE`）；「已发货」不代表分享操作触发真实发货，不实现任何物流规则。

### 3.3 兑换码失败态（D-017）

- 提交按钮仅在输入为空时 disabled，不用位数拦提交，避免把未定稿位数变成硬校验。
- 输入自动转大写（`hint` 明示不区分大小写）。
- 四类失败态各自有 `?state=` 直达：`format` / `invalid` / `used` / `network`，反馈文案取自 `REDEEM_FEEDBACK`，以 `role="status"` 居中提示呈现。
- 提交中态 700ms 固定时长，确定性可截图。

## 4. 实现映射

| 关注点 | 文件 |
| --- | --- |
| T009 区段状态/浮层与 owner 标注 | [routes.ts](../../../src/app/router/routes.ts) |
| 卡券/分享对象/分享商品/核销凭证/兑换码规则夹具 | [fixtures/index.ts](../../../src/app/fixtures/index.ts) |
| 卡包三态与使用弹层（#54 #62 #63 #64） | [Card.tsx](../../../src/pages/Card.tsx) |
| 分享与分享成功（#65 #66） | [CardShare.tsx](../../../src/pages/CardShare.tsx) |
| 确认核销 / 已核销 / 重复核销（#67） | [ConfirmVerify.tsx](../../../src/pages/ConfirmVerify.tsx) |
| 兑换码与成功入包（#68 #69） | [Redeem.tsx](../../../src/pages/Redeem.tsx) |
| 状态调试面板（D-020，验收工具） | [DebugPanel.tsx](../../../src/components/mobile/DebugPanel.tsx) |

## 5. 状态与夹具矩阵

| 节点 | 可达 URL |
| --- | --- |
| #54 卡包（默认＝可用） | `/card` |
| #54 使用弹层 | `/card?coupon=c1&overlay=use` |
| #62 卡包-可用 | `/card?state=available` |
| #63 卡包-已使用（空态） | `/card?state=used` |
| #64 卡包-已过期 | `/card?state=expired` |
| #65 分享（搭子列表单选） | `/card/share?coupon=c1` |
| #66 分享成功 | `/card/share?coupon=c1&state=success` |
| #67 扫码 | `/card/verify` |
| #67 消费密码核销 | `/card/verify/password` |
| #67 确认核销 | `/card/verify/confirm` |
| #67 已核销 | `/card/verify/confirm?state=done` |
| #67 重复核销 | `/card/verify/confirm?state=repeat` |
| #68 兑换卡券（默认） | `/redeem` |
| #68 格式错误 / 无效 / 已使用 / 网络异常 | `/redeem?state=format` / `invalid` / `used` / `network` |
| #69 兑换成功 | `/redeem?state=success` |

上表每条 URL 都可以在 **`?debug=1` 下** 页面右下角的「调试」胶囊里直接切换，无需手改地址栏（D-020、D-021）：`/card?debug=1` 提供 4 个状态胶囊 + 2 个弹层胶囊，`/redeem?debug=1` 提供 6 个状态胶囊，`/card/share`、`/card/verify/confirm` 各按 `routes.ts` 登记项渲染。不带 `debug=1` 的正常页面完全不渲染面板，避免胶囊遮挡底部操作区；面板切换状态时 `debug=1` 原样保留。

## 6. 工程证据

- `npm run typecheck`（`tsc --noEmit`）退出码 0。
- `npm run build` 通过：CSS 48.37 kB（gzip 9.96 kB）/ JS 361.85 kB（gzip 108.19 kB）。构建耗时随缓存冷热波动（实测 4.4s–24.5s），不作为验收指标。
- 截图采集：`scripts/capture-t009.mjs`（375×812），28 张产物见 `docs/workbench/evidence/screenshots/t009-*.png`，其中 5 张为调试面板展开态（`t009-debug-panel-*`，均在 `?debug=1` 下采集）。
- 控制台：**无 error；存在既有 Router warning**（40 条 React Router v7 future flag，每次导航 2 条），属项目全局既有提示、非本轮引入，脚本用 `isKnownNoise` 单列计数，不混入问题清单。
- 运行时断言（脚本输出）：
  - #65 分组标题为原型的「搭子列表」（count=1）；默认选中「小美」；搜索「室友」命中 1 位；改选为「室友小婷」。
  - #66 副文案为原型的「已成功分享给好友，邀请他也来一起玩吧」；`aria-label="分享的商品"` 商品卡渲染「洗发试用装 / 2024.01.08 / 已发货 / 单次使用」四字段；「返回」后 URL 回到 `/card/share?coupon=c1`。
  - #67 确认核销后跳转 `/card?state=used`。
  - #68 空输入时提交按钮 `disabled=true`；输入自动转大写 `DRCARD000888`；四类失败反馈文案逐条命中。
  - 调试面板门控：`/card`（无 `debug=1`）`[data-debug-panel]` count=0；`/card?debug=1` 面板可见；点「卡包-已过期」→ `/card?debug=1&state=expired`（`debug=1` 保留）且选中 Tab 同步为「已过期」；点「默认」→ `/card?debug=1`（`state` 清空、`debug=1` 保留）；点「使用方式选择」后 `使用体验券` 弹层可见且 `debug=1` 保留；`/redeem` 无 `debug=1` 时 count=0，`/redeem?debug=1` 切到「兑换失败-兑换码无效」后反馈为「兑换码无效，请核对后重试」且 `debug=1` 保留。
- 脚本约定：壳层 TitleBar 自带 `<h1>`、返回箭头自带 `aria-label="返回"`，页面级定位一律收敛到 `page.getByRole('main')`（`PageContainer` 渲染 `<main>`，调试面板也在其内），否则会撞 Playwright strict mode。
- 调试面板背景曾整块失效：`bg-surface-inverse/95` 这类斜杠透明度对 `tailwind.config.js` 里注册为裸 `var(--color-…)` 的颜色无效（缺 `<alpha-value>` 占位符），Tailwind 不产出背景色。改为不带 alpha 的 `bg-surface-inverse`（与 ComDesign `Toast`/`Snackbar` 同款写法），未触碰 Token 与配置。

## 7. 已关闭阻塞项

用户本轮定案，以下三项从未决转为已关闭，后续不得再当作待确认：

| 编号 | 定案内容 |
| --- | --- |
| `B-015` | 按原型做「搭子列表」夹具：单选对象 → 下一步 → 分享成功。不做对方接受、次数限制、时效限制和持久化；分享后原卡包状态暂不改变。 |
| `B-016` | 卡包以 `reference/卡包.html` 为准，只保留「可用 / 已使用 / 已过期」三个 Tab，不加「全部」和「使用记录」。`docs/prototype/04-mall-card-order.md` §8 的四 Tab 描述不再作为该页依据。 |
| `B-017` | `洗发试用装 / 已发货 / 单次使用` 仅作为原型展示夹具。「已发货」不代表分享操作触发真实发货，不实现物流规则。 |

## 8. 未决与风险

- `REDEEM_CODE_RULE.pending` 保持 `true`：12 位来自标准页，但原型仍存在 8/11 位表达（受 D-004 约束），接入真实兑换服务前不得当作最终规则。
- 真实二维码、扫码硬件、短信与服务端核销仍不在范围（T009 卡「不在范围」），当前为明确模拟适配层。
- 「已使用」空态依赖夹具中无 `used` 卡券；一旦产品确认核销后应写回卡包，需要同时决定持久化方式，届时 D-015 需重新评估。
- B-015 定案「不做持久化、原卡包状态不变」意味着分享成功后回卡包仍能看到原券。这是当轮定案的预期行为，不是缺陷；若后续要做真实转移，需要新决策而非按缺陷修复。
