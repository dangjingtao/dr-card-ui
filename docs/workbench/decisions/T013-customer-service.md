# 决策记录｜T013 福利官服务项、客服会话行为与排队状态边界

> 决策日期：2026-08-22
> 决策卡：[T013-customer-service.md](../tasks/T013-customer-service.md)
> 决策类型：UI / Flow / Integration
> 决策范围：Mockplus 节点 #57、#58、#70、#71
> 决策人：用户在本轮任务中明确指定三项口径，其余施工细节由智能体在「未确认即不实现」前提下决定
> 状态：Accepted

## 1. 决策目的

T013 长期卡在三个阻塞项上：

- `B-012` 品牌福利官服务项：原型 3 类 vs 历史稿 T08 的 4 服务卡；
- `B-013` 智能客服聊天视觉、热门问题、关键词识别行为；
- `B-014` 人工客服排队：只保留排队中/已接入两态，队列递减等 mock 不得引入。

用户在本轮任务中直接给出前两项与第三项的判定口径：福利官按摹客 3 类服务纠正历史 4 项；智能客服只做欢迎、对话、发送失败/重试、企微客服入口；#70 只做「排队中 / 已接入」两个确定状态，不做人数递减、倒计时或虚假队列。

本记录据此关闭 `B-012`，并把 `B-013` / `B-014` 中**已获口径的部分**关闭、**仍缺原型依据的部分**降级为夹具登记项，使 T013 可在不倒推产品规则的前提下交付四个节点的确定状态。

## 2. 决策结论

| 编号 | 决策 | 依据 | 关联节点 |
| --- | --- | --- | --- |
| D-038 | 品牌福利官服务列表为 **人工客服 / 活动咨询 / 福利抽奖 3 类**，历史稿 T08 的 4 项（每日签到提醒 / 会员专享福利 / 新品体验优先购 / 节日活动预告）废弃 | 用户在本轮任务中明确「按摹客的 3 类服务，纠正现有历史稿的 4 项服务」 | #57 |
| D-039 | 智能客服只实现 **欢迎、对话、发送失败/重试、企微客服入口** 四件事；历史稿的 4 个「热门问题」快捷入口不实现 | 用户在本轮任务中限定了这四项；热门问题清单在原型 §9 无依据，登记于 `CHAT_RULE_STATUS.quickQuestions` | #58 |
| D-040 | 发送成败由 **关键词确定性映射** 决定（`CHAT_FAIL_KEYWORDS` 命中即 `failed`，否则 `sent`），不使用随机数、不做概率失败 | 全项目夹具禁随机，`?state=` 直达必须可复现；随机失败无法作为交互证据 | #58 |
| D-041 | #70 只有 **排队中 / 已接入** 两个由 `?state=` 驱动的确定状态；不做人数递减、倒计时、虚假队列，也不编造坐席后续问答 | 用户在本轮任务中明确禁止；坐席问答在原型 §11 无依据，登记于 `CHAT_RULE_STATUS.agentReplies` | #70 |
| D-042 | 智能客服历史消息**保留**进入 #70 当前页面，接入后仅追加一条坐席开场语；历史消息头像保持说话人「诗」，坐席消息用「哥」区分，不把历史说话人改写为人工客服 | 原型 §11 明确「智能客服历史消息保留在当前页面中」，不另起完整客服系统；头像随页面整体改写会伪造历史发言人 | #70 |

`B-012` 由 D-038 关闭；`B-013` 的视觉与关键词行为由 D-039/D-040 关闭，热门问题清单本体仍缺原型依据；`B-014` 由 D-041 关闭。

## 3. 决策细节

### 3.1 福利官 3 类服务（D-038）

服务项文案与徽标来自 `WELFARE_OFFICER_SERVICES`。原型 §8 未给出副标题与跳转目标，故渲染为**说明列表而非可点按钮**，避免造出未确认的下级页面；该缺口登记在 `WELFARE_OFFICER_RULE_STATUS`。

### 3.2 智能客服四件事（D-039、D-040）

- 欢迎态首屏**只有一条**小诗欢迎语，不预置历史对话、不铺热门问题；
- 用户消息经 `sending`（`CHAT_SEND_LATENCY_MS = 700`）落到确定性结果，成功后追加 `CHAT_BOT_FALLBACK_REPLY` 兜底回答 —— 原型未给出问答库，故不假装能答具体问题；
- 失败气泡带 `CHAT_SEND_FAILED_HINT` 与「重试」，重试复用同一条消息文本再走一次判定；`CHAT_FAILED_MESSAGES` 的用户消息刻意**不含**失败关键词，以保证「失败 → 重试 → 成功」可被证据脚本走通；
- 输入「人工客服 / 转人工 / 人工」等同于点击页内入口，直接打开 #71（`isChatHumanRequest`）；
- 壳层 `MobileLayout` 的 `titleBarAction` 只支持 `'settings' | 'notifications'`，故「企微客服」入口渲染在**页面体内**而非标题栏右上角。

### 3.3 #71 企微引导弹层（D-041）

原型 §10 的弹层只列出「取消」，因此**弹层内只实现「取消」**。#71 → #70 的前进动作原型未给出，属未确认业务规则，按项目硬规则**不实现**；仅登记于 `CHAT_RULE_STATUS.humanQueueEntry`。#70 本轮暂通过直达路由 `/service/chat/human?state=queuing|connected` 与 `?debug=1` 调试面板验收，不伪造正式业务入口。`scripts/capture-t013.mjs` 对此加了**负向断言**（弹层内「已添加，进入人工客服」按钮数必须为 0），防止该未确认入口被重新加回。

企微为外部能力，本项目不做真实跳转，仅以 `WecomQrPlaceholder` 占位（`role="img"` + 明确的「QR Placeholder」字样），不伪装成可扫码的真二维码。

### 3.4 排队两态与历史保留（D-041、D-042）

- `?state=queuing`：`LoadingIndicator` + 固定文案 `CHAT_QUEUE.queuing`，**文案在任意时长后都不变化**；输入区 `disabled`，placeholder 明示「接入人工客服后可继续发送」；
- `?state=connected`：坐席条 + 一条开场语；已接入态发送**只回显用户自己的消息**，不生成坐席回复；
- 两态均不带取消排队按钮 —— 原型未画，且取消后的目标态无依据。

## 4. 运行证据

`BASE_URL=http://127.0.0.1:5210 node scripts/capture-t013.mjs`（exit 0，16 张 375×812 截图，输出至 `docs/workbench/evidence/screenshots/t013-*.png`）：

| 断言 | 实测 | 对应决策 |
| --- | --- | --- |
| #58 欢迎态消息数 | 1（无 4 热门问题） | D-039 |
| 发送中气泡数 / 一轮往返后消息数 | 1 / 3 | D-039 |
| `?state=failed` 点重试后失败气泡数 | 0（失败 → 重试 → 成功可复现） | D-040 |
| 输入含失败关键词后失败气泡数 | 1（确定性可复现） | D-040 |
| #71 弹层按钮清单 | `["取消"]` | D-041 |
| #71 未确认的「已添加，进入人工客服」按钮数 | 0（负向断言，预期 0） | D-041 |
| #71 取消后弹层残留 / URL | 0 / 无 query | D-041 |
| #70 排队态直达 URL | `/service/chat/human?state=queuing`（暂由直达路由验收） | D-041 |
| 排队 4 秒后状态区文案 | 不变（无递减/倒计时） | D-041 |
| 排队中输入区 `isDisabled()` | true | D-041 |
| 已接入态坐席开场语头像含「哥」 | true | D-042 |
| 已接入态历史小诗消息头像仍为「诗」 | true | D-042 |
| 调试面板（无 `debug=1` / 有 `debug=1`） | 0 / 1 | 全局约束 |
| 320×480 发送按钮底边 / 视口高 | 468 / 480（未被遮挡） | 验收标准 |

`BASE_URL=http://127.0.0.1:5210 node scripts/verify-reference-pages.mjs`：T013 全部 7 条路径 `OK`，`console errors: NONE`。

`npm run build`（内含 `tsc --noEmit`）exit 0。

capture 脚本报告的 20 条 warning 全部为 React Router v6→v7 future flag 提示（`v7_startTransition` / `v7_relativeSplatPath`，各 10 条），`[assert]` 计数为 0；该 warning 项目既有、跨页面普遍存在，非本卡引入，不阻塞。

## 5. 仍未确认（登记在 `CHAT_RULE_STATUS`，未实现）

| 键 | 阻塞 | 内容 |
| --- | --- | --- |
| `quickQuestions` | B-013 | 热门问题清单本体（历史稿 4 项已废弃，原型未给替代清单） |
| `botAnswers` | B-013 | 智能客服问答库；现仅一条兜底回答 |
| `wecomJump` | B-013 | 企微真实跳转/拉起方式与失败回退目标 |
| `queueDynamics` | B-014 | 队列人数、等待时长与推进规则 |
| `humanQueueEntry` | B-014 | #71 → #70 的前进动作（原型 §10 弹层只有「取消」，未给前进入口）；未确认故不实现 |
| `agentReplies` | B-014 | 已接入后坐席的后续问答行为 |
